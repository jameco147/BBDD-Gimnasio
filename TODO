Hay que seguir el formato, ejemplo:

tabla (column1, column2)
    primary key {column1}
    foreign key {column1} -> tabla{columnx}



persona (id, nombre, apellido, f_nacimiento, dni, direccion, email, telefono)
    primary key {id}
